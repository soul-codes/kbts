# save

```ts
/**
 * Save the result of `render` to the filesystem.
 * @param result
 * @param rootDir
 */
function save(
  result: readonly OutputFile[],
  rootDir: string | undefined
): Promise<void>;

```
